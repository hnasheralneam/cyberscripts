### Make dirs for every box and put sourcefiles in there
Example: `mkdir web`
 - Make sure the name of the dir is the same as the second value in the hostfile
 - Current required sourcefile is `port-sources`
